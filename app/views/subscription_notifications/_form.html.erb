<%= form_with(model: subscription_notification, local: true) do |form| %>
  <% if subscription_notification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subscription_notification.errors.count, "error") %> prohibited this subscription_notification from being saved:</h2>

      <ul>
      <% subscription_notification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
